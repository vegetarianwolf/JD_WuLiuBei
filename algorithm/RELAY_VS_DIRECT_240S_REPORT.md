# 240 秒 Relay vs Direct 配对墙钟对比实验报告

日期：2026-08-15
环境：Windows 11 / Python 3.13.3；200 tasks / 8 UAV / capacity=2 / K=25
配置：seed 2026080500–2026080502（3 种子配对），candidate_limit=48（rank 24 / exact 48），
初始解 = regret-2 直送构造（三种子共享同一初始路线）
Relay 网络：2 个中继站，每任务 1 个候选站，绕行比 1.3，选址种子 42
运行方式：`run_relay_comparison.py --output-dir results/relay_comparison_240s --wall-seed-count 3 --wall-time-limit 240 --wall-safety-margin 2`
判优规则：严格词典序（late_count → total_lateness_min → distance_km），逐种子配对比较

## 1. 主要结果

| 指标（3 种子均值） | Direct | Relay（2 站分阶段） | 对比 |
|---|---:|---:|---|
| late_count | **66.00** | 66.00 | 平 |
| total_lateness_min | **2146.83** | 2151.84 | Direct 优 |
| distance_km | 627.55 | **627.04** | Relay 略优 |
| it/s（真实，含构造） | **29.18** | 25.17 | Direct 快 15.9% |
| 采用中继的任务数 | — | 0 / 200 | 未采用 |
| 跨机接驳次数 | — | 0 | — |
| 中继总等待时间（min） | — | 0 | — |
| 全局评估次数（relay） | — | 22,000/run 左右 | — |

**配对胜率：Direct 3 : 0 Relay**（tie 0）。三种子下 Relay 均未在词典序第一/第二目标上
胜出；尽管 Relay 在 seed 2026080501 上总里程更短（625.45 vs 633.27），但总逾期更大
（2186.84 vs 2182.37），按词典序仍判 Direct 胜。

## 2. 逐种子结果

| Seed | Direct 得分 | Relay 得分 | Relay 任务 | 胜者 |
|---:|---|---:|---:|---|
| 2026080500 | `(65, 2121.11, 631.74)` | `(65, 2123.01, 635.53)` | 0 | Direct |
| 2026080501 | `(66, 2182.37, 633.27)` | `(66, 2186.84, 625.45)` | 0 | Direct |
| 2026080502 | `(67, 2137.02, 617.63)` | `(67, 2145.66, 620.13)` | 0 | Direct |
| **均值** | **`(66.00, 2146.83, 627.55)`** | **`(66.00, 2151.84, 627.04)`** | **0** | **Direct 3:0** |

三种子的 Relay 阶段均未在最终解中采用任何中继任务（relay_task_count=0），
Relay 搜索阶段的最佳得分与其 DIRECT 预热得分完全一致（`relay_warmup_score` 等于
最终得分），说明在 2 站/1 候选/绕行 1.3 的默认 Relay 空间下，Relay 阶段未能找到
优于 DIRECT 预热的词典序解。

## 3. 吞吐与预算

| Seed | Direct it/s | Relay it/s | Direct 迭代 | Relay 迭代 |
|---|---:|---:|---:|---:|
| 2026080500 | 29.24 | 24.83 | 6,903 | 5,862 |
| 2026080501 | 29.24 | 25.72 | 6,904 | 6,072 |
| 2026080502 | 29.06 | 24.96 | 6,862 | 5,895 |
| **均值** | **29.18** | **25.17** | **6,890** | **5,943** |

- 两种子变体均耗满 238 s 墙钟预算（含 ~1.9 s 构造时间），符合配对预算设计。
- Relay 单迭代成本更高（beam 构建 + 全局评估），吞吐约为 Direct 的 86%。
- Direct 平均完成 6,890 次迭代，Relay 平均 5,943 次，Direct 多 ~16%。

## 4. 分析

1. **词典序质量**：三种子下两变体的 `late_count` 完全相同（65/66/67），差异集中在
   `total_lateness_min`。Direct 总逾期均值低 5.01 分钟，三种子全部按词典序胜出。
2. **中继未被采用**：默认 Relay 空间（2 站、1 候选、绕行 1.3、sample_every=6）在
   240 s 内没有产生优于 DIRECT 的解。
3. **总里程**：Relay 均值总里程略短（627.04 vs 627.55），但总逾期更大，按词典序
   无法翻盘。这与"Relay 通过绕行换运力余量"的预期一致——在当前紧耦合实例上，
   额外绕行没有转化为词典序收益。
4. **确定性**：三条轨迹均为确定性算法；同 seed 重跑解一致（已记录 solution SHA-256）。
5. **与历史对比**：旧配置（3 站、2 候选、sample_every=4）240 s 下 Direct 亦为 3:0；
   本次默认配置（2 站、1 候选、sample_every=6）结果一致，且三种子 Relay 任务数均为 0，
   说明更轻量的默认 Relay 空间更倾向退化为 DIRECT。

## 5. 结论

- 在当前实现与默认参数下，**200 任务规模 240 s 预算内 Direct 模式在词典序质量与
  吞吐上均占优（3:0）**；Relay 未在最终解中采用任何中继任务。
- 后续若需 Relay 在质量侧胜出，可尝试更多中继站/更高绕行容忍/更大 relay probe
  预算，或在中继友好的数据集（任务聚集、跨机紧约束）上对比。

## 6. 可审计性

- 求解器源码 SHA-256：`b8ff6e9fb2a83b91a951b5c0163d10fcf725425cd33fd8c1e73bc15d18dbc64a`
- 输入 CSV SHA-256：`386d3fbaa9009aeed947493782d657ed0b30b25d369c8187bfc9619fc7e31d25`
- 初始路线 SHA-256（三种子相同）：`0aa05645d9ca61227c51c1d69993de896ecca045136165dec06a4bd1636288b6`
- 原始记录：`results/relay_comparison_240s/`（`relay_comparison_results.json`、
  `relay_comparison_runs.csv`、`run_solutions/` 内 6 份完整路线）。
